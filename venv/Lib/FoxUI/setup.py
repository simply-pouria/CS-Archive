from setuptools import setup, find_packages

setup(
    name='FoxUI',
    version='0.1',
    packages=find_packages(),
    py_modules=['FoxUI'],
    install_requires=[
        # لیست وابستگی‌های پروژه
    ],
)
